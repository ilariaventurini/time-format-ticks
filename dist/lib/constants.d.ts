import { Options } from "./types";
declare type name = string;
declare type ms = number;
declare type TimeInterval = Array<[name, ms]>;
export declare const TIME_INTERVALS: TimeInterval;
export declare const defaultOptions: Required<Options>;
export {};
